language: python

services:
    - docker
    
before_install:
    - docker-compose up &
   
install:
    - pip install -r ./test/requirements.txt
    - docker run -it csec380project_server chown $USER:$USER /var/www/html
    
script:
    pytest ./test
